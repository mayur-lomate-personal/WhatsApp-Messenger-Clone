server:
  port: 8092
spring:
  application:
    name: authoriser-server
jwt:
  secret: mayur
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true